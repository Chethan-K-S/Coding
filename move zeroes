void moveZeroes(int* nums, int length) {
    int x=0;
    for(int i=0;i<length;i++){
        if (nums[i]!=0){
            nums[x]=nums[i];
            x++;
        }
        }
    for(int i=x;i<length;i++){
        nums[i]=0;
    }
}
